# Data Files

官方竞赛数据不要提交到 GitHub。

请在本地 `data/` 目录放置以下文件：

- `recipes_sample_2000.csv`
  - 编码：GBK
  - 字段：`名称`、`食材清单`、`烹饪步骤`、`label`
- `50个用户健康档案（脱敏）.json`
  - 必须使用详细版本，包含 `身高_cm`、`体重_kg`、`BMI` 和 `体检指标` 字段。
- `对话用例.json`

后续代码会默认从该目录读取数据；如果用户档案缺少上述详细字段，程序会直接报错，避免误用旧版简化档案。
